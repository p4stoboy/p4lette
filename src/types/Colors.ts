

export type HexColor = string;
export type RGBColor = {r: number, g: number, b: number};
export type HSLColor = {h: number, s: number, l: number};
export type Colors = {hex: HexColor, rgb: RGBColor, hsl: HSLColor};